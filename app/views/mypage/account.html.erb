<div class="ui container mypage">
  <div class="ui grid owner-edit form">
    <div class="four wide column">
      <i>혹시 계정을 탈퇴하고 싶으신가요?</i>
      <p><%= button_to "Cancel my account",
       registration_path(resource_name), class: "ui button", data: { confirm: "Are you sure?" }, method: :delete %></p>
    </div>
    <div class="nine wide column owner-account">
      <div class="ui attached message">
        <div class="header">계정관리</div>
        <p>매장 주인의 정보가 바뀌었나요?</p>
      </div>
      <div class="ui form attached fluid segment">
        <%= form_for(@owner, url: registration_path(@owner), html: { method: :put }) do |f| %>
          <%= devise_error_messages! %>
          <%= render partial: "layouts/ownerform", locals: {f: f, owner_edit: true} %>
        <% end %>
      </div>
    </div>
  </div>
</div>
